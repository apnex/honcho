apiVersion: v1
kind: Service
metadata:
  name: vip-honcho
  annotations:
    metallb.universe.tf/allow-shared-ip: "host"
spec:
  type: LoadBalancer
  selector:
    app: honcho
  ports:
    - name: http
      port: 8000
      targetPort: 8000
